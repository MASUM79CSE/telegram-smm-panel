/** Escapes text for Telegram's HTML parse mode to prevent markup injection from user-controlled strings. */
export function escapeHtml(text: string): string {
  return text
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;");
}
