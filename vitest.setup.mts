// Global test setup — currently a no-op placeholder. Kept as a separate
// file (rather than inlined in vitest.config.ts) so it's easy to add
// global mocks/matchers later without touching the config.
export {};
